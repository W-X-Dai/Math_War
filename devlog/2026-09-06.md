# 2026-09-06 — 彈頭命中同步修正（0.8.1）

## 問題與修正

- 集中遊戲設定時，水平與落下彈頭的飛行時間從原本的 `0.52`／`0.56` 秒誤寫為 `5`／`1.5` 秒。恢復原始次秒級時序，避免已鎖定的移動敵人在彈頭抵達前不合理地漏進基地，並加入精確設定與基地邊界回歸測試。
- 彈頭現在會逐步套用目標公式卡的群聚水平／垂直位移；目標消失後，該枚彈頭會在剩餘動畫期間沿用最後一次位置，避免跳回未錯位的模擬座標。快取只保留仍存在的彈頭，不會在無限模式持續累積。
- 移除依彈頭自身寬度計算的敵卡邊界錨點，改以彈頭中心對準目標；命中光圈會在同幀敵人移動後繼續貼住未死亡的目標。
- 版本提升為 `0.8.1`。沒有存檔、關卡 seed、公開介面或部署遷移影響。

## 驗證

- `npm test`：9 個測試檔全部通過，新增彈頭時序、近基地移動目標、命中光圈跟隨、群聚位移、邊界座標及快取清理覆蓋。
- `npm run build`：正式版建置通過並更新 `dist/`。
- Headless Chrome/CDP（`1440×900`）：實際確認 `x²` 在 `0.52` 秒飛行期間不變，命中後成為 `2x`；三隻同路群聚時，彈頭命中點位於目標公式卡內，目標與命中效果座標一致，基地未受傷，且沒有 console error、framework overlay 或空白頁。
- Browser 外掛、in-app browser 與專案 Playwright 均不可用，因此沿用 repository 既有的本機 Headless Chrome/CDP 驗證方式。

---

# 2026-09-06 — 慢速彈頭生命週期修正（0.8.2）

## 問題與修正

- 確認 `5`／`1.5` 秒是刻意設定的水平／落下彈頭速度，推翻 0.8.1 將其視為誤植的判斷；新增精確設定測試，避免後續再次加速。
- 水平彈頭改為發射後沿路線獨立前進，不再由發射時的敵人擁有。以連續掃掠碰撞計算同一幀內的相遇點，並在每次命中後重新評估其餘彈頭；前方敵人先被消去時，後續彈頭不會憑空消失，而會命中同路下一隻活敵人。
- 歸零敵卡立即離場，不再等待指向它的彈頭。當同路已無敵人，水平彈頭仍會保持原 ID 飛到戰場右邊界之外才清除；波次也會等待這些彈頭離場後再換段。
- 由上方落下的單體武器維持施放時的目標鎖定；原目標提前消失時只會落空，不會改打相同位置的新敵人。漏進基地的敵人同樣立即移除。
- 版本提升為 `0.8.2`。沒有存檔、關卡 seed、公開介面或部署遷移影響。

## 驗證

- `npm test`：完整測試通過；涵蓋慢速設定、同路雙敵續撞、同幀多彈重算、2× 速度掃掠碰撞、歸零字卡立即移除、最後一怪後彈頭完整離場、落下武器不轉移目標，以及既有數學與關卡行為。
- `npm run build`：正式版建置通過並更新 `dist/`。
- Headless Chrome/CDP（`1440×900`，一般與減少動態效果模式）：逐幀確認第一隻敵人歸零後 DOM 立即移除，後續水平彈頭保持相同 ID 向右前進並命中第二隻敵人；最後一怪消失後，孤立彈頭仍飛出右邊界才清除。頁面無 console error、framework overlay 或空白畫面。
- Browser 外掛、in-app browser 與專案 Playwright 均不可用，因此沿用 repository 既有的本機 Headless Chrome/CDP 驗證方式。

---

# 2026-09-06 — JSON 關卡進度備份（0.9.0）

## 功能與設計

- 選關頁新增「下載進度 JSON」與「載入進度 JSON」；檔名使用玩家本地日期，載入後會同步更新畫面與既有 localStorage。
- 存檔採 `math-zombie-progress`、檔案版本 `1` 的 envelope，內層沿用既有關卡進度版本，沒有 localStorage migration。這份檔案只保存通關與解鎖狀態，不保存進行中的戰鬥、seed、成績或教學略過選擇。
- 匯入會嚴格驗證 JSON、檔案類型、版本與連續關卡順序；亂序、重複、未知、過新、損壞或超過 64 KB 的檔案不會覆蓋目前進度。若 localStorage 寫入失敗，仍保留本次工作階段載入的進度並顯示警告。
- 完整通關檔會選取無限模式，部分進度檔會選取下一個尚未完成的已解鎖關卡。版本依向後相容的新功能提升為 `0.9.0`。

## 驗證

- `node --test tests/progress.test.js tests/campaign.test.js`：2 個測試檔全部通過；涵蓋版本化 roundtrip、嚴格拒絕無效資料、reactive state／localStorage 套用、完整進度、檔案大小、儲存失敗與本地日期檔名。
- `npm run build`：正式版建置通過並更新 `dist/`。
- Headless Chrome/CDP（`1440×900`、`390×844`）：下載兩關進度、清空後上傳恢復為 `2 / 6`、選取下一關、持久化到 localStorage，並驗證破損 JSON 不會覆寫進度；三個檔案操作皆可見、上傳控制可由鍵盤聚焦、手機版沒有水平溢位，也沒有 console error 或 framework overlay。
- 完整 `npm test`：10 個測試檔全部通過；彈頭碰撞工作流的呈現斷言也已更新完成。
- Browser 外掛、CUA 瀏覽器與專案 Playwright 都不可用，因此未新增依賴，沿用系統既有的 Headless Chrome/CDP 驗證。

---

# 2026-09-06 — 參數捲軸與跨路線單體施放（0.10.0）

## 功能與設計

- 把 `P(x)−k`、定積分、`f(k)`、`xD+kI` 與 `D²+kI` 五種參數武器從占用路線的砲台改為一次性捲軸；`D` 與 `D²` 維持可重複攻擊、可擋路的通用砲台。
- 組裝常數後，玩家點擊空白捲軸即可刻寫參數；定積分依序刻寫下界與上界。完成後再點捲軸與任意路線的單一敵人，便由上方落下並在命中時套用算子。完成的捲軸不能覆寫，取消、無效目標、施放前運算失敗或算力不足不會消耗；通過檢查並發射後若目標提前消失，捲軸仍依既有落下武器規則消耗並落空。
- 關卡生成器改按敵人本體、等式護盾與分裂子代的實際使用次數供應參數捲軸及精確公式／`k`；可重複攻擊的砲台仍按路線去重。對數敵人的正式反制改為 `f(1)`，避免 `D → Euler(1)` 受慢速水平彈頭時序干擾。
- 保留既有 operator ID；`installAssembly` 引擎入口名稱仍可使用，但目的地改為軍械 queue 的捲軸 ID。沒有 localStorage、進度 JSON 或後端資料遷移。參數武器的種類由砲台改為單體目標武器，屬向後相容的玩法能力調整，版本提升為 `0.10.0`。

## 驗證

- `npm test`：10 個測試檔全部通過；引擎、生成器、平衡、設定與彈頭測試涵蓋單／雙參數刻寫、不可覆寫、跨正式段保存、跨路線選敵、無效施放不消耗、命中後消耗，以及本體／護盾／分裂子代的逐張保障供應。
- `npm run build`：正式版建置通過並更新 `dist/`。
- Headless Chrome/CDP（`1440×900`）：從第三關整備實際組出 `1`、刻寫 `xD+1I`、開波並對 `x⁻¹` 施放；已刻寫牌顯示正確，發射時消耗一張牌與 `Σ40`，落下彈頭鎖定原敵人並將其消去，全程沒有建立 Euler 塔。頁面標題與內容正常，沒有水平溢位、console error、資源失敗或 framework overlay。
- Computer Use 環境沒有可用瀏覽器，專案也沒有 Playwright，因此未新增依賴，沿用 repository 既有的本機 Headless Chrome/CDP 驗證方式。

---

# 2026-09-06 — 群聚敵人水平彈頭對位修正（0.10.1）

## 問題與修正

- 同一路敵人的公式卡會以呈現層水平／垂直錯位避免重疊，但慢速水平彈頭在飛行期間只沿未錯位的模擬路線前進，直到碰撞後才跳到受擊卡片，因此兩隻同時生成時會看似打在兩者之間。
- 水平彈頭現在依自己與目前碰撞候選目標的相對距離，逐步套用該卡片的群聚位移；物理碰撞、前方目標消失後的重新選敵，以及落下武器的鎖定規則都維持不變。
- 同座標敵卡不再以字串 ID 額外排序，改為保留生成順序，與引擎在同位置、同碰撞時間時的穩定優先順序一致。
- 版本提升為 `0.10.1`。沒有關卡 seed、戰鬥平衡、存檔、進度 JSON 或部署遷移影響。

## 驗證

- `node --test tests/projectile.test.js` 與完整 `npm test`：10 個測試檔全部通過；新增水平彈頭逐步靠向目前目標，以及目標離場後保留位置快照的覆蓋。
- `npm run build`：正式版建置通過並更新 `dist/`。
- Codex in-app browser（`1191×926`）：第一關壓力段建立同路雙怪群聚並逐幀暫停量測；前方卡片範圍為 x `687.2–751.2`、y `358.0–398.0`，彈頭在碰撞前已進入該卡，命中光圈位於 x `734.5`、y `378.0`，且 `impactTargetId` 對應前方敵人。頁面內容正常，沒有 console warning/error 或 framework overlay。

---

# 2026-09-06 — 軍械 Queue 卡片固定欄寬（0.10.2）

## 問題與修正

- 桌面版軍械卡原本使用可伸展的 `1fr` 欄寬，queue 數量減少時，剩餘卡片會突然變寬；保障補給使卡片超量時又會縮回最小寬度。
- 卡片欄寬改為隨戰場容器響應、但不隨卡片數量變化的 `clamp(96px, 7.5cqw, 118px)`；超出可視區的卡片維持既有水平捲動。`900px` 以下原有的固定 `132px` 緊湊版規則保持不變。
- 版本提升為 `0.10.2`。沒有戰鬥、關卡 seed、存檔或介面相容性影響。

## 驗證

- 新增 presentation 回歸測試，鎖定桌面固定響應欄寬、禁止恢復依卡片數量分配的 `1fr`，並保留緊湊版 `132px` 規則。
- `npm test`：11 個測試檔全部通過；`npm run build` 通過並更新 `dist/`。
- Codex in-app browser（`1280×720`）：第一關壓力段從 11 張超量 Queue 實測到部署四座塔後剩 7 張；兩個狀態下每張卡都維持 `96px`，水平捲動仍可用，且沒有 console warning/error 或 framework overlay。

---

# 2026-09-06 — 無限捲軸、數字工房與 z 偏微分（0.11.0）

## 功能與設計

- 砲台與捲軸改為獨立供應：`D` 與 `D²` 繼續使用容量八張的砲台 queue，所有已解鎖 target/global 捲軸則常駐在 `scrollLibrary` 且不會耗盡。成功發射只扣算力；參數捲軸發射後清空參數槽，下次重新刻寫。
- 公式工房改為數字鍵盤直接組合常數，支援負號、小數、`π`、退格、清除與輸入框 Enter，並沿用五格常數庫與單／雙參數刻寫流程。舊 formula/constant queue API 與 seeded supply 字段保留為內部相容邊界，玩家 UI 不再依賴它們。
- 玩家可見的第二自由變數由 `y` 改為 `z`；序列化仍保留 `yPower` 作為相容鍵，數學核心新增 `z` 為該次方的 alias。全場偏微分改為真正的 `∂/∂z`，預覽、彈頭、教學與公式顯示同步更新。
- 強力捲軸以算力作為主要限制：`lim x→∞` 調整為 `Σ600`，全場 `∂/∂z` 調整為 `Σ1200` 並保留每波一次限制。這是向後相容的玩法能力與平衡變更，版本提升為 `0.11.0`；無 localStorage、進度 JSON 或部署遷移。

## 驗證

- `npm test`：11 個測試檔全部通過；新增數字常數、無限極限捲軸重複施放、參數捲軸發射後重置、價格層級、`z` alias 與全場 `∂/∂z` 預覽／命中覆蓋。
- `npm run build`：Vite 正式版建置通過並更新 `dist/`。
- Browser plugin 不可用，repository 也未安裝 Playwright；改用系統 Headless Chrome/CDP 驗收 `1280×720` 與 `900×720`。實際進入第一關，點擊 `π` 與「組合並存入常數」後出現 `π` 庫存與成功 toast；砲台卡顯示數字快捷鍵，捲軸顯示 `∞`/「無限」，兩個視窗均無頁面水平溢位、空白頁或 framework overlay。

---

# 2026-09-06 — 獨立捲軸庫與常數算式解析（0.12.0）

## 功能與設計

- 戰場上方軍械區拆成左側「一般武器工房」與右側「無限捲軸庫」，兩邊有各自的標題、計量與水平滾動容器。資料層原有 `operatorQueue` / `scrollLibrary` 邊界保持不變。
- 公式工房改為四欄算式鍵盤，支援加減、乘除、括號、π與平方根。輸入框可辨識 `*` / `/` 及受限的 `\times`、`\cdot`、`\div`、`\pi`、`\sqrt{...}`，也支援嵌套根號與常用隱式乘法。
- 新解析器使用白名單 tokenizer 與 recursive descent，沒有 `eval` 或任意 JavaScript 執行。除零、負數實數平方根、未閉合括號、未支援的 LaTeX 與非有限結果都會在預覽階段被拒絕。引擎仍只接收已計算且通過上限驗證的數值。
- 工房內按鈕取得焦點時，數字鍵不再誤選上方 `1–8` 砲台牌。這是向後相容的 UI 與輸入能力新增，版本提升為 `0.12.0`；無存檔、關卡 seed、數值 schema 或部署遷移。

## 驗證

- `npm test`：12 個測試檔全數通過；新增運算優先序、括號、一元負號、π、平方根、受限 LaTeX、隱式乘法，以及除零、負數開根、殘缺算式與未支援輸入的回歸覆蓋。
- `npm run build`：Vite 正式版建置通過並更新 `dist/`；`git diff --check` 通過。
- Browser plugin 不可用，repository 也未安裝 Playwright；改用系統 Headless Chrome/CDP 驗收 `1280×720`、`900×720` 與 `520×720`。畫面按鍵輸入 `12÷3×2` 預覽為 `8`，LaTeX `\sqrt{81} \div 3` 預覽為 `3`，兩者都成功存入常數庫；工房按鈕聚焦後按 `1` 不會誤選砲台。第一章單一捲軸庫會擴展到整個軍械區，注入測試砲台後會分成 `operatorQueue` / `scrollLibrary` 兩區；各視窗均無頁面水平溢位、console warning/error 或 framework overlay。

---

# 2026-09-06 — 常數入門與大型算術武器（0.13.0）

## 功能與設計

- 保留既有六章順序與 `foundation` ID，將第一章改為純常數入門：正式段只生成 `±1`～`±4`，負數用加法、正數用減法精準歸零。第一章不解鎖砲台，補牌與軍械 UI 明確支援只有捲軸的章節。
- 新增加法捲軸，並把微分砲延後到第二章；第二章同時解鎖高算力的乘法、除法、開根大型捲軸。乘數與除數不可為 `0`，避免通用秒殺與未定義運算；開根只接受目前符號模型可精確表示的非負完全平方單項式。
- 加減法維持每次 `Σ25`，大型算術捲軸為 `Σ300`～`Σ450`，定位是降低危險係數或次方的前處理，最後仍需把敵式化為 `0`。所有新武器沿用無限捲軸庫、單體落下彈頭與發射前驗證。
- 第一章的 seeded 內容與教學波已更新，但章節 ID、進度 JSON、localStorage 與 expression 序列化格式不變；版本依向後相容玩法新增提升為 `0.13.0`。

## 驗證

- `npm test`：12 個測試檔全部通過；包含算術領域規則、500-seed 第一章純常數與逐路反制、無砲台補牌、無限捲軸施放／失敗不扣算力，以及彈頭文字與設定契約。
- `npm run build`：Vite 正式版建置通過並更新 `dist/`。
- Codex in-app browser（預設桌面視窗與 `900×720`）：確認選關頁第一章只列常數、加法與減法；實際進入教學後只顯示兩張基礎捲軸且沒有空的砲台工房或補給倒數。組出 `2`、刻寫減法、對常數 `2` 發射後，推導紀錄顯示 `2 → 0` 並消去；頁面沒有 console warning/error 或 framework overlay。

---

# 2026-09-06 — 軍械與常數拖放施放（0.14.0）

## 功能與設計

- 所有可立即使用的砲台與捲軸都可從軍械區拖到目的地：砲台拖到可部署格、單體捲軸拖到敵人；全場偏微分拖到戰場後仍保留既有確認步驟。
- 空白參數捲軸不可拖到戰場。玩家需先把常數庫 token 拖到捲軸刻寫；定積分依 `lowerBound`、`upperBound` 順序接收兩次拖放，完成後才開放施放拖曳。點擊與鍵盤流程保留為無障礙相容操作。
- 引擎新增以明確卡片 ID 執行的原子部署／施放入口。失敗不會扣算力、消耗其他卡片或誤用目前選取的常數；垃圾桶仍只接受砲台牌與常數，不會因捲軸現在可拖曳而誤判。
- 這是向後相容的操作能力新增，版本提升為 `0.14.0`。存檔、進度 JSON、關卡 seed、數學規則與既有點擊 API 均不變。

## 驗證

- `npm test`：12 個測試檔全部通過；引擎與 presentation 測試涵蓋明確砲台卡部署、已刻寫捲軸直接施放、空白捲軸拒絕施放、拖入指定常數、雙參數依序刻寫，以及 DOM 拖放契約。
- `npm run build` 通過並更新 `dist/`；`git diff --check` 通過。
- Codex in-app browser（`1280×720`）確認砲台與無參數捲軸可拖、空白參數捲軸不可拖、常數 token 可拖，刻寫後捲軸會立即變成可拖狀態；頁面沒有 console warning/error 或 framework overlay。此瀏覽器控制介面的座標拖曳未送出原生 HTML5 drag/drop 事件，因此事件路由以引擎回歸測試與 DOM 契約覆蓋，未宣稱自動化原生拖曳通過。

---

# 2026-09-06 — 可拖曳常數圓盤（0.15.0）

## 功能與設計

- 公式工坊的可視鍵盤收旂為 12 顆圓盤：`0`–`9`、`π` 與 `e`。圓盤點擊時仍可組成多位數，退格、清除與文字輸入框保留為獨立編輯工具。
- 任一圓盤可直接拖到空白參數捲軸刻寫；它們是可重複使用的 copy 來源，不占用、不消耗常數庫，也不會被垃圾桶接受。複合算式則沿用「組合後存入常數庫」的一次性刻寫流程。
- 常數解析器新增精確的小寫 `e` 與 `\mathrm{e}`，並支援 `2e`、`πe` 等既有隱式乘法；大寫 `E` 與未定義的科學記號仍會拒絕。公用 formatter 以 `e`、`−e` 等精確符號顯示，不會在捲軸或彈頭上誤顯示為 `2.72`。
- 這是向後相容的交互能力新增，版本提升為 `0.15.0`。存檔、進度 JSON、關卡 seed、數學規則與既有點擊／常數庫操作不變。

## 驗證

- `npm test`：12 個測試檔全數通過；覆蓋 e 解析與精確顯示、圓盤單／雙槽刻寫、常數庫不消耗、拖放 DOM 契約與垃圾桶隔離。
- `npm run build` 通過並更新 `dist/`；`git diff --check` 通過。
- Codex in-app browser（`1191×926`）確認 12 顆圓盤以三欄、四列一次完整顯示，沒有圓盤內捲動；點擊 `e` 後輸入與預覽都顯示精確的 `e`。此瀏覽器控制介面未能執行原生 HTML5 drag/drop，因此圓盤刻寫路由以引擎回歸測試與 DOM 拖放契約覆蓋，未宣稱自動化原生拖曳通過。
